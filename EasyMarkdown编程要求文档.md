## 一、项目概述

本项目包含两个关联的子项目：

- **子项目一（Web 版）：** 基于浏览器运行的 Markdown 在线编辑网站，用户无需安装任何软件即可通过网络访问并使用全部编辑功能。

- **子项目二（Desktop 版）：** 基于 Electron 框架构建的本地桌面应用程序（支持 Windows、macOS、Linux），用户安装后可在本地环境中编辑 Markdown 文件，具备文件系统访问权限。

两个子项目共用核心编辑与预览逻辑，但在运行环境、文件管理机制、部署方式等方面有各自独立的需求。

## 二、Web 版功能需求

### 2.1 浏览器兼容性

- 支持主流现代浏览器：Google Chrome（最新版及近两个大版本）、Mozilla Firefox（最新版及近两个大版本）、Microsoft Edge（最新版及近两个大版本）、Safari（最新版及近两个大版本）。

- 页面需采用响应式布局，在不同屏幕分辨率（最小支持宽度 768px）下保证基本可用性。

### 2.2 部署方式

- 采用纯静态部署方案，所有页面资源（HTML、CSS、JavaScript）可托管于任意静态文件服务器或 CDN，无需服务端运行时。

## 一、项目概述

本项目包含两个关联的子项目：

- **子项目一（Web 版）：** 基于浏览器运行的 Markdown 在线编辑网站，用户无需安装任何软件即可通过网络访问并使用全部编辑功能。

- **子项目二（Desktop 版）：** 基于 Electron 框架构建的本地桌面应用程序（支持 Windows、macOS、Linux），用户安装后可在本地环境中编辑 Markdown 文件，具备文件系统访问权限。

两个子项目共用核心编辑与预览逻辑，但在运行环境、文件管理机制、部署方式等方面有各自独立的需求。

## 二、Web 版功能需求

### 2.1 浏览器兼容性

- 支持主流现代浏览器：Google Chrome（最新版及近两个大版本）、Mozilla Firefox（最新版及近两个大版本）、Microsoft Edge（最新版及近两个大版本）、Safari（最新版及近两个大版本）。

- 页面需采用响应式布局，在不同屏幕分辨率（最小支持宽度 768px）下保证基本可用性。

### 2.2 部署方式

- 采用纯静态部署方案，所有页面资源（HTML、CSS、JavaScript）可托管于任意静态文件服务器或 CDN，无需服务端运行时。

- 网站本身不依赖后端服务，但可酌情对接第三方 CDN 用于加速常用库的加载。

### 2.3 文件管理

- 支持通过浏览器原生文件对话框打开本地 `.md` 文件并加载至编辑器。

- 支持将当前编辑内容保存为 `.md` 文件下载到本地。

- 具备自动保存功能：在用户编辑过程中定期将内容保存至浏览器本地存储（LocalStorage 或 IndexedDB），防止意外关闭页面导致内容丢失[](https://onlinemarkdown.com/zh-cn)。

- 支持新建文件（清空编辑器并重置为空白文档状态）。

- 支持文件重命名功能（在界面中直接修改文件名）。

### 2.4 数据隐私

- 所有用户编辑内容仅在用户本地浏览器中处理，不上传至任何远程服务器。

- 需在界面适当位置明确告知用户“数据仅存储在本地”，增强用户信任。

## 三、Desktop 版功能需求

### 3.1 技术框架

- 使用 Electron 框架构建跨平台桌面应用。

- 主进程负责文件系统操作（读取、写入、目录遍历等），渲染进程负责界面展示和用户交互，两者之间通过 Electron 的 IPC 机制通信。

- 启用上下文隔离（Context Isolation）以提高安全性，文件系统相关 API 通过 preload 脚本安全暴露给渲染进程。

### 3.2 文件管理

- 支持通过系统原生文件对话框打开单个 Markdown 文件（`.md`、`.markdown` 等格式）。

- 支持**打开文件夹**功能：选择一个文件夹后，以侧边栏树形结构展示该文件夹下的所有 Markdown 文件，用户可点击树形列表中的文件进行切换编辑。文件夹树仅展示 `.md` 和 `.markdown` 文件，忽略其他类型的文件。

- 支持**新建文件**：在已打开的文件夹路径下创建新的 Markdown 文件。

- 支持**保存**（覆盖原文件）和**另存为**（选择新路径保存）。

- 支持**最近打开文件**列表：在菜单栏中展示用户最近打开过的文件路径，点击可直接快速打开。

- 支持文件拖放操作：将 `.md` 文件从操作系统文件管理器拖入应用窗口，即可打开该文件。

### 3.3 自动保存

- 默认开启自动保存功能，在用户停止输入一定时间（建议 300ms 防抖延迟）后自动将内容写入磁盘。

- 自动保存状态需在界面中明确指示（如状态栏显示“已保存”或“保存中…”）。

### 3.4 窗口管理

- 支持窗口最小化、最大化、关闭等标准桌面窗口操作。

- 支持记住上次窗口位置和大小，启动时恢复。

- 支持多标签页功能吗？——本项目桌面版初期以单文档编辑为主，暂不要求多标签页功能。

## 四、编辑器主界面布局（Web 版与 Desktop 版通用）

### 4.1 分栏布局

- 主屏幕采用左右双栏布局：左侧为 Markdown 源文件编辑区，右侧为实时渲染预览区。

- 左右两栏宽度默认各占 50%，用户可通过拖动中间分隔条自由调整左右比例。

- 分栏布局需支持**隐藏预览**模式：用户可通过菜单命令或快捷键切换为仅显示编辑区的单栏模式；再次执行相同操作可恢复双栏显示。[](https://onlinemarkdown.com/zh-cn)

### 4.2 编辑区

- 编辑区为纯文本编辑器（或代码编辑器），支持 Markdown 语法高亮显示（标题、粗体、斜体、代码块、链接、图片等语法用不同颜色/样式区分）。

- 支持标准文本编辑操作：撤销（Ctrl+Z）、重做（Ctrl+Y 或 Ctrl+Shift+Z）、剪切、复制、粘贴、全选。

- 支持行号显示（可在设置中开启或关闭）。

- 编辑器应支持等宽字体，默认推荐使用 `Consolas`、`Menlo`、`Monaco`、`Courier New` 或 `Source Code Pro` 等编程字体。

- 编辑区字体大小可由用户调整（默认 14px）。

### 4.3 预览区

- 预览区实时渲染 Markdown 内容为 HTML 并展示。渲染引擎推荐使用 GitHub Flavored Markdown（GFM）标准[](https://onlinemarkdown.com/zh-cn)。

- 预览区与编辑区之间实现**滚动同步**：当用户滚动编辑区时，预览区自动滚动到对应内容位置；反之亦然（双向同步）。

- 预览区样式应与 GitHub 的 Markdown 渲染风格保持一致，包括表格、代码块、引用块等的样式。

- 预览区支持：
  
  - 代码块语法高亮（支持至少 50 种以上常见编程语言）。
  
  - 表格渲染。
  
  - Mermaid 图表渲染（流程图、时序图、甘特图等）[](https://onlinemarkdown.com/zh-cn)。
  
  - LaTeX 数学公式渲染（行内公式和块级公式）。
  
  - 任务列表（Task List）渲染，支持复选框样式。
  
  - 脚注（Footnote）渲染。

- 预览区图片引用支持相对路径和绝对路径，Desktop 版中相对路径应以当前文件所在目录为基准解析。

### 4.4 状态栏

- 界面底部设置状态栏，显示以下信息（可配置显示/隐藏）：
  
  - 当前文件名。
  
  - 文档字数统计、字符数统计、段落数统计。
  
  - 保存状态指示（“已保存”/“未保存”/“保存中…”）。
  
  - 当前光标所在行号和列号。

## 五、菜单栏功能（Web 版与 Desktop 版通用，但部分菜单项因平台而异）

### 5.1 文件菜单

| 菜单项      | 功能说明                          | Web 版          | Desktop 版     |
| -------- | ----------------------------- | -------------- | ------------- |
| 新建       | 创建新的空白 Markdown 文档            | ✓              | ✓             |
| 打开文件     | 通过文件对话框打开 `.md` 文件            | ✓（浏览器对话框）      | ✓（系统对话框）      |
| 打开文件夹    | 选择文件夹并以树形结构展示所有 Markdown 文件   | ✗              | ✓（专属功能）       |
| 最近打开文件   | 展示最近打开过的文件列表并支持快速打开           | ✓（基于浏览器本地存储记录） | ✓（基于本地文件路径记录） |
| 保存       | 保存当前文档                        | ✓（下载到本地）       | ✓（覆盖原文件）      |
| 另存为      | 将文档保存为新文件                     | ✓（下载到本地）       | ✓（选择新路径保存）    |
| 打印       | 打印当前 Markdown 文档的渲染结果         | ✓              | ✓             |
| 打印预览     | 在打印前预览渲染效果                    | ✓              | ✓             |
| 导出为 HTML | 将当前 Markdown 内容导出为独立的 HTML 文件 | ✓（下载）          | ✓（保存到本地）      |
| 导出为 PDF  | 将当前 Markdown 内容导出为 PDF 文件     | ✓（下载）          | ✓（保存到本地）      |

### 5.2 编辑菜单

编辑菜单包含以下项目，所有项目均支持键盘快捷键：

| 菜单项  | 功能说明        | 快捷键（Windows/Linux）    | 快捷键（macOS）      |
| ---- | ----------- | --------------------- | --------------- |
| 撤销   | 撤销上一步操作     | Ctrl+Z                | ⌘+Z             |
| 重做   | 重做已撤销的操作    | Ctrl+Y 或 Ctrl+Shift+Z | ⌘+Y 或 ⌘+Shift+Z |
| 剪切   | 剪切选中文本      | Ctrl+X                | ⌘+X             |
| 复制   | 复制选中文本      | Ctrl+C                | ⌘+C             |
| 粘贴   | 粘贴剪贴板内容     | Ctrl+V                | ⌘+V             |
| 全选   | 选择全部内容      | Ctrl+A                | ⌘+A             |
| 查找   | 在当前文档中查找文本  | Ctrl+F                | ⌘+F             |
| 查找替换 | 查找并替换文本     | Ctrl+H                | ⌘+H             |
| 隐藏预览 | 切换预览区的显示/隐藏 | Ctrl+Shift+P          | ⌘+Shift+P       |

### 5.3 视图菜单

| 菜单项     | 功能说明                   |
| ------- | ---------------------- |
| 显示/隐藏行号 | 切换编辑区行号显示              |
| 放大字体    | 增大编辑器字体大小              |
| 缩小字体    | 减小编辑器字体大小              |
| 重置字体大小  | 恢复默认字体大小               |
| 切换主题    | 在预设主题之间切换（浅色主题 / 深色主题） |

### 5.4 帮助菜单

| 菜单项            | 功能说明                                         |
| -------------- | -------------------------------------------- |
| Markdown 入门用法  | 打开入门教程，介绍 Markdown 的基本概念、用途和快速上手指南           |
| Markdown 语法速查表 | 打开完整的 Markdown 语法参考，包含所有标准语法和扩展语法（GFM）的说明与示例 |
| 常用工具           | 列出推荐的 Markdown 相关工具和资源（如其他编辑器、转换工具等）         |
| 快捷键列表          | 展示当前应用所有可用键盘快捷键的完整列表                         |
| 关于             | 显示应用名称、版本号、许可证信息、开发者信息等                      |

## 六、格式化工具栏（Web 版与 Desktop 版通用）

### 6.1 快速格式化按钮

编辑区上方设置格式化工具栏，包含以下快捷按钮。点击按钮可将对应的 Markdown 语法插入编辑区光标位置，或对选中的文本包裹相应的格式化语法：

| 按钮                 | 功能                                            | Markdown 语法示例        |
| ------------------ | --------------------------------------------- | -------------------- |
| 粗体（Bold）           | 将选中文本设为粗体                                     | `**文本**`             |
| 斜体（Italic）         | 将选中文本设为斜体                                     | `*文本*`               |
| 删除线（Strikethrough） | 为选中文本添加删除线                                    | `~~文本~~`             |
| 标题（Heading）        | 插入不同级别的标题（下拉选择 H1–H6）                         | `# 标题` 到 `###### 标题` |
| 无序列表               | 插入无序列表项                                       | `- 列表项`              |
| 有序列表               | 插入有序列表项                                       | `1. 列表项`             |
| 任务列表               | 插入任务列表项                                       | `- [ ] 任务`           |
| 代码块                | 插入代码块                                         | ` ```语言\n代码\n``` `   |
| 行内代码               | 将选中文本设为行内代码                                   | `` `代码` ``           |
| 引用块                | 插入引用块                                         | `> 引用内容`             |
| 表格                 | 插入预设结构的表格模板                                   | 自动生成表头和分隔行           |
| 链接                 | 插入链接（弹出对话框输入 URL 和文本）                         | `[文本](URL)`          |
| 图片                 | 插入图片（弹出对话框输入图片 URL 和替代文本；Desktop 版支持选择本地图片文件） | `![替代文本](URL)`       |
| 水平线                | 插入水平分隔线                                       | `---` 或 `***`        |
| 上标                 | 将选中文本设为上标                                     | `^{文本}`              |
| 下标                 | 将选中文本设为下标                                     | `_{文本}`              |

### 6.2 工具栏键盘快捷键

除按钮外，以下格式化操作支持键盘快捷键（与常见编辑器的习惯保持一致）：

| 操作   | 快捷键（Windows/Linux）       | 快捷键（macOS） |
| ---- | ------------------------ | ---------- |
| 粗体   | Ctrl+B                   | ⌘+B        |
| 斜体   | Ctrl+I                   | ⌘+I        |
| 删除线  | Ctrl+Shift+S             | ⌘+Shift+S  |
| 行内代码 | Ctrl+`\| ⌘+`             |            |
| 代码块  | Ctrl+Shift+`\| ⌘+Shift+` |            |
| 链接   | Ctrl+K                   | ⌘+K        |
| 图片   | Ctrl+Shift+I             | ⌘+Shift+I  |
| 引用块  | Ctrl+Shift+Q             | ⌘+Shift+Q  |
| 无序列表 | Ctrl+Shift+U             | ⌘+Shift+U  |
| 有序列表 | Ctrl+Shift+O             | ⌘+Shift+O  |

### 6.3 图片插入（Desktop 版增强功能）

- Desktop 版在插入图片时，除支持输入图片 URL 外，还应支持“选择本地文件”选项，用户可通过文件对话框选择本地图片文件。

- 选择本地图片后，自动将其复制到当前 Markdown 文件所在目录下的 `images` 子文件夹中，并插入相对路径引用。

- 如果 `images` 文件夹不存在，自动创建该文件夹。

## 七、国际化（多语言切换）

### 7.1 支持的语言

应用界面（菜单、按钮、标签、提示信息等）需支持切换以下主要语言：

- 简体中文（zh-CN）

- 繁体中文（zh-TW）

- 英语（en-US）

- 日语（ja-JP）

- 韩语（ko-KR）

- 法语（fr-FR）

- 德语（de-DE）

- 西班牙语（es-ES）

- 葡萄牙语（pt-BR）

- 俄语（ru-RU）

- 阿拉伯语（ar-SA）

- 印地语（hi-IN）

### 7.2 多语言实现方案

- 所有界面文本统一存放在各语言对应的 JSON 或 JavaScript 文件中，以键值对形式管理。

- 语言切换即时生效，无需刷新页面或重新启动应用。

- 语言偏好记忆：用户选择的语言存储于本地存储，下次打开页面或启动应用时自动使用上次选择的语言。

- 默认语言根据浏览器 `navigator.language` 自动检测并设置，若检测到的语言不在支持列表中则默认使用英语。

- 语言切换控件置于工具栏或菜单栏的醒目位置（如下拉选择框或图标按钮）。

### 7.3 语言文件覆盖范围

语言翻译需覆盖以下界面元素：

- 所有菜单项及子菜单项。

- 格式化工具栏按钮的提示文字。

- 状态栏标签和信息。

- 对话框（如“打开文件”、“保存文件”）中的按钮和提示文字。

- 帮助菜单下的所有教程和速查表内容。

- 错误提示与警告信息。

- “关于”窗口内容。

## 八、Markdown 入门教程与语法速查表

### 8.1 入门用法

- 内容以 HTML 页面或模态窗口形式呈现，包含以下章节：
  
  1. 什么是 Markdown（简介）。
  
  2. Markdown 的优势与应用场景。
  
  3. 快速上手示例（最基本的标题、段落、粗体、斜体等用法演示）。
  
  4. 编辑器的基本使用说明（如何进行编辑和预览、如何使用工具栏等）。

- 入门教程中的 Markdown 示例应支持实时预览，即左侧为示例 Markdown 代码，右侧为对应渲染效果。

### 8.2 语法速查表

- 提供一份完整的 Markdown 语法参考，以表格形式列出所有支持的语法元素，包含：
  
  - 语法名称。
  
  - 语法说明。
  
  - 源码示例。
  
  - 渲染效果预览。

- 语法速查表至少覆盖以下内容：
  
  - 标题（H1–H6）。
  
  - 粗体、斜体、删除线、上标、下标。
  
  - 无序列表、有序列表、任务列表、嵌套列表。
  
  - 代码块（含语言标识）、行内代码。
  
  - 引用块、嵌套引用。
  
  - 链接（行内链接、参考式链接、自动链接）。
  
  - 图片（行内图片、参考式图片）。
  
  - 表格（含对齐方式）。
  
  - 水平线。
  
  - 脚注。
  
  - Mermaid 图表的基本语法。
  
  - LaTeX 数学公式（行内公式和块级公式）。

- 速查表应支持搜索功能，用户输入关键词后快速过滤出相关语法条目。

### 8.3 常用工具

- 帮助菜单中的“常用工具”页面列出以下内容：
  
  - 推荐的 Markdown 在线编辑器（附链接）。
  
  - 推荐的桌面 Markdown 编辑器（附链接）。
  
  - Markdown 转 PDF/HTML 工具推荐。
  
  - 图床服务推荐（便于插入图片）。
  
  - 其他辅助工具和资源。

- 每个推荐工具应包含名称、简要描述和官方链接。

## 九、预览与打印功能

### 9.1 打印功能

- 打印时以预览区的渲染结果作为打印内容，排版应与预览区一致。

- 打印样式需通过独立的打印 CSS 控制，确保打印输出美观（合适的字体大小、页边距、分页处理等）。

- 打印时自动隐藏页面中不必要的界面元素（如菜单栏、工具栏、状态栏等），仅保留文档内容。

### 9.2 打印预览

- 调用浏览器或系统的打印预览功能，展示实际打印效果。

- 用户在打印预览中可以调整打印设置（纸张大小、页边距等）。

## 十、导出功能

### 10.1 导出为 HTML

- 将当前 Markdown 文档渲染为完整的独立 HTML 文件。

- 生成的 HTML 文件应包含内联样式，确保在没有网络连接的情况下依然能正确显示渲染效果（即不依赖外部 CSS 文件）。

- 导出的 HTML 文件名默认与 Markdown 文件名相同（仅扩展名更改）。

### 10.2 导出为 PDF

- Web 版：通过浏览器打印功能将渲染结果导出为 PDF。

- Desktop 版：可通过 Electron 的打印 API 或第三方 PDF 生成库，将渲染结果直接保存为 PDF 文件。

- 导出的 PDF 文件应保留正确的排版和代码高亮样式。

## 十一、非功能性需求

### 11.1 性能

- 编辑器加载时间（从打开页面到可编辑状态）：Web 版应在 2 秒以内，Desktop 版应在 3 秒以内。

- 预览渲染延迟：用户输入内容后，预览区应在 300ms 内完成更新（采用防抖机制）。

- 大文档编辑：编辑区在打开 10 万字符以上的 Markdown 文档时，不应出现明显卡顿或响应延迟。

### 11.2 可用性

- 所有功能均支持键盘操作和快捷键，满足无障碍访问的基本要求。

- 工具栏按钮需用真实的 HTML 按钮元素（而非 `<div>` 模拟），以支持屏幕阅读器。

- 预览区更新时，应通过 `aria-live` 区域向屏幕阅读器推送状态更新信息。

### 11.3 安全性

- Web 版：对预览区渲染的 HTML 内容进行安全过滤，防止 XSS 攻击。使用 HTML 净化库（如 DOMPurify）对 Markdown 渲染输出的 HTML 进行清理，移除危险标签（如 `<script>`）和危险属性（如事件处理属性）。

- Desktop 版：文件系统操作限制在用户明确选择的目录范围内，不得随意访问其他系统路径。启用上下文隔离，避免渲染进程直接访问 Node.js API。

## 十二、技术选型建议（供开发团队参考，不做强制要求）

### 12.1 代码编辑器

推荐使用以下成熟的代码编辑器组件：

- **CodeMirror**：功能全面、插件丰富，适合在线编辑器场景。

- **Monaco Editor**（VS Code 的核心编辑器）：提供出色的编辑体验和语法高亮，但体积较大。

- **Ace Editor**：轻量级编辑器，适合对加载速度要求较高的场景。

### 12.2 Markdown 渲染引擎

推荐使用以下渲染库：

- **marked**：轻量、快速、支持 GFM，是使用最广泛的 Markdown 解析库之一。

- **markdown-it**：插件化架构，功能丰富，支持自定义扩展。

- **remark**：基于 AST 的 Markdown 处理器，适合需要高级定制功能的场景。

### 12.3 前端框架

- **React** 或 **Vue.js**：推荐用于 Web 版和 Desktop 版（Electron）的 UI 构建。

- **Electron**：用于构建 Desktop 版跨平台桌面应用。

### 12.4 代码高亮

推荐使用 **highlight.js** 或 **Prism.js** 为预览区代码块提供语法高亮，需支持至少 50 种以上编程语言。

## 十三、版本与阶段规划

### 13.1 第一阶段（MVP）

- 实现左右分栏布局与实时预览。

- 实现基础文件管理（新建、打开、保存）。

- 实现格式化工具栏（粗体、斜体、标题、列表、代码块、链接、图片、表格）。

- 实现多语言切换（至少支持中、英两种语言）。

- 实现帮助菜单下的“Markdown 语法速查表”。

- Web 版优先完成；Desktop 版基于 Web 版核心代码通过 Electron 封装实现。

### 13.2 第二阶段

- 实现打开文件夹功能（Desktop 版）。

- 实现最近打开文件列表。

- 实现打印与打印预览。

- 实现导出 HTML 和 PDF。

- 实现深色主题模式。

- 补充 Mermaid 图表和 LaTeX 公式渲染支持。

- 完成所有计划语言的翻译。

### 13.3 第三阶段

- 实现移动端适配（响应式布局优化，小屏幕下自动切换为单栏模式）。

- 添加更多辅助功能和自定义配置选项（如自定义字体、自定义快捷键等）。

- 根据用户反馈持续优化性能与体验。

## 十四、测试检测要求与流程

### 14.1 基础功能测试清单

以下为每个功能模块的必测项，开发完成后须逐项验证：

#### 14.1.1 编辑器基础

| 测试项   | 验证方法            | 预期结果                    |
| ----- | --------------- | ----------------------- |
| 编辑器加载 | 打开页面            | CodeMirror 编辑器正常显示，光标可见 |
| 文本输入  | 在编辑区输入文字        | 文字正常显示，无延迟              |
| 文本粘贴  | Ctrl+V 粘贴内容     | 内容正确粘贴到编辑区              |
| 撤销/重做 | Ctrl+Z / Ctrl+Y | 操作正确撤销和重做               |
| 行号显示  | 默认状态            | 左侧行号正常显示                |
| 字体大小  | 菜单放大/缩小         | 编辑器字体大小变化               |

#### 14.1.2 实时预览

| 测试项        | 验证方法           | 预期结果          |
| ---------- | -------------- | ------------- |
| 基础渲染       | 输入 `# 标题`      | 预览区显示 H1 标题   |
| GFM 语法     | 输入粗体、斜体、列表等    | 正确渲染为 HTML    |
| 代码高亮       | 输入带语言标识的代码块    | 代码有语法高亮       |
| Mermaid 图表 | 输入 mermaid 代码块 | 正确渲染流程图       |
| LaTeX 公式   | 输入 `$E=mc^2$`  | 正确渲染数学公式      |
| 滚动同步       | 滚动编辑区          | 预览区同步滚动       |
| 防抖更新       | 快速输入           | 预览区 300ms 内更新 |

#### 14.1.3 格式化工具栏

| 测试项   | 验证方法      | 预期结果             |
| ----- | --------- | ---------------- |
| 粗体按钮  | 选中文本，点击粗体 | 文本被 `**...**` 包裹 |
| 斜体按钮  | 选中文本，点击斜体 | 文本被 `*...*` 包裹   |
| 标题下拉  | 点击标题按钮    | 显示 H1-H6 选项      |
| 列表按钮  | 点击无序/有序列表 | 插入列表前缀           |
| 代码块按钮 | 点击代码块     | 插入 ` ``` ` 代码块   |
| 链接按钮  | 点击链接      | 弹出链接对话框          |
| 图片按钮  | 点击图片      | 弹出图片对话框          |
| 表格按钮  | 点击表格      | 弹出表格行列对话框        |

#### 14.1.4 菜单系统

| 测试项   | 验证方法      | 预期结果            |
| ----- | --------- | --------------- |
| 菜单展开  | 点击"文件"菜单  | 下拉菜单正常显示所有选项    |
| 菜单项文字 | 查看所有菜单项   | 文字正确显示（非空白或翻译键） |
| 菜单切换  | 展开后点击另一菜单 | 切换到新菜单          |
| 菜单关闭  | 点击空白区域    | 菜单关闭            |

#### 14.1.5 文件管理

| 测试项    | 验证方法      | 预期结果              |
| ------ | --------- | ----------------- |
| 新建文件   | 菜单 → 新建   | 编辑器清空，文件名重置       |
| 打开文件   | 菜单 → 打开文件 | 文件对话框弹出，选择后内容加载   |
| 保存文件   | Ctrl+S    | 触发浏览器下载           |
| 自动保存   | 输入内容后等待   | localStorage 自动保存 |
| 页面刷新恢复 | 刷新页面      | 恢复上次编辑内容          |

#### 14.1.6 国际化

| 测试项  | 验证方法           | 预期结果             |
| ---- | -------------- | ---------------- |
| 语言切换 | 右上角选择"English" | 所有菜单、按钮、状态栏切换为英文 |
| 中文切换 | 选择"简体中文"       | 所有界面切换为中文        |
| 语言记忆 | 刷新页面           | 保持上次选择的语言        |
| 菜单翻译 | 切换语言后打开菜单      | 菜单项文字正确翻译        |

#### 14.1.7 主题与布局

| 测试项  | 验证方法           | 预期结果          |
| ---- | -------------- | ------------- |
| 深色主题 | 菜单 → 视图 → 切换主题 | 背景变深色，文字变浅色   |
| 浅色主题 | 再次切换           | 恢复浅色主题        |
| 分栏拖拽 | 拖动中间分隔条        | 左右面板宽度变化      |
| 隐藏预览 | Ctrl+Shift+P   | 隐藏右侧预览，仅显示编辑器 |

#### 14.1.8 状态栏

| 测试项   | 验证方法   | 预期结果       |
| ----- | ------ | ---------- |
| 文件名显示 | 打开文件   | 状态栏显示文件名   |
| 字数统计  | 输入文字   | 字数和字符数实时更新 |
| 光标位置  | 点击不同位置 | 行号列号正确显示   |
| 保存状态  | 编辑后查看  | 显示"未保存"    |

#### 14.1.9 帮助系统

| 测试项   | 验证方法               | 预期结果     |
| ----- | ------------------ | -------- |
| 入门教程  | 帮助 → Markdown 入门用法 | 弹窗显示教程内容 |
| 语法速查表 | 帮助 → 语法速查表         | 弹窗显示语法参考 |
| 快捷键列表 | 帮助 → 快捷键列表         | 显示所有快捷键  |
| 常用工具  | 帮助 → 常用工具          | 显示推荐工具列表 |
| 关于    | 帮助 → 关于            | 显示版本信息   |

### 14.2 测试流程

#### 第一轮：静态检查

1. 检查所有文件是否存在（HTML、CSS、JS）
2. 检查 JS 语法正确性（`node --check` 或浏览器控制台）
3. 检查 CSS 变量定义完整性
4. 检查 CDN 资源可访问性

#### 第二轮：功能测试

1. 按 14.1 清单逐项测试
2. 记录每项测试结果（通过/失败/部分通过）
3. 失败项记录错误现象和控制台错误信息

#### 第三轮：集成测试

1. 完整编辑流程：新建 → 输入 → 格式化 → 预览 → 保存
2. 文件操作流程：打开 .md 文件 → 编辑 → 另存为
3. 语言切换 → 所有界面元素验证
4. 主题切换 → 所有区域样式验证

#### 第四轮：边界测试

1. 大文档测试（10万字符以上）
2. 特殊字符输入（emoji、中文、日文、阿拉伯文）
3. 连续快速操作（快速切换菜单、快速输入）
4. 浏览器刷新后状态恢复

### 14.3 已知问题修复记录

| 日期         | 问题                   | 原因                           | 修复方案                            |
| ---------- | -------------------- | ---------------------------- | ------------------------------- |
| 2026-05-03 | 菜单项显示空白，编辑器无法输入      | 全局模块使用 `const` 声明，跨脚本作用域不可访问 | 所有模块改为 `var` 声明                 |
| 2026-05-03 | 中文"取消"按钮显示英文         | i18n 翻译键值错误                  | 修正 `dialog.cancel` 为 `取消`       |
| 2026-05-03 | marked v12 代码高亮不生效   | `highlight` 选项已弃用            | 改为渲染后调用 `hljs.highlightElement` |
| 2026-05-03 | KaTeX 公式不渲染          | DOMPurify 删除 `data-math` 属性  | 添加到 `ADD_ATTR` 白名单              |
| 2026-05-03 | Desktop 版 require 报错 | Electron 渲染进程禁用 Node.js      | 改用字符串处理路径                       |
